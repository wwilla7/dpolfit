name: test
channels:
  - openeye
  - conda-forge
dependencies:
    # Base depends
  - python
  - pip
  - numpy
  - scipy
    #  - jinja2
    # - pydantic
  - openff-utilities
  - openff-units >=0.2
  - openff-recharge
    #  - matplotlib
  - pint
  - openff-toolkit
    #- openmm =8
  - psi4
    #- cudatoolkit >=0.11
  - openeye-toolkits
    #- sqlalchemy
    #- sqlite
  
    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
    - ray
      #- python-lsp-server
    - numpyencoder
  #  - codecov

