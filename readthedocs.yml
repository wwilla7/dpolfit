# readthedocs.yml

version: 2

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

build:
  os: ubuntu-20.04
  image: latest

python:
  version: 3.10
  install:
    - method: pip
      path: .

conda:
  environment: devtools/conda-envs/docs_env.yaml

formats:
  - pdf
